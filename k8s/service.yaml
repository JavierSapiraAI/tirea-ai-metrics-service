apiVersion: v1
kind: Service
metadata:
  name: metrics-service
  namespace: langfuse
  labels:
    app: metrics-service
    component: metrics
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3001
    targetPort: http
    protocol: TCP
  selector:
    app: metrics-service
